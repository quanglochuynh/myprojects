float bX, bY;
int vel;

void initialize(){
    bX = random(width);
    bY = random(height);
    ellipse(bX, bY, 50, 50);
}

void setup(){
    size(800,600);
    noStroke();
    background(50);
}

void draw(){

}
